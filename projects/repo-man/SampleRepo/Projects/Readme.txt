The `Projects` directory houses all of the active projects. We don't have a
separate repository for each project because we want it to be easy for
developers to work on multiple projects at multiple times and for code to be
shared among projects easily by branching.

